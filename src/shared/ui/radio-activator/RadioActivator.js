/* Web component that enables and disables classes or attributes as a
radio list; when clicked on a child element, the parent (this module)
enables attributes and classes on that element and disables them on others. */

/* Веб-компонент, включающий и выключающий классы или атрибуты в виде
радиосписка; при щелчке на дочернем элементе родительский (данный модуль)
включает атрибуты и классы для этого элемента и выключает их для других. */

export class RadioActivator extends HTMLElement {
  constructor() {
    super();
  }
}
